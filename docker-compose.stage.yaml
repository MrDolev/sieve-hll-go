# STAGE environment overlay. Used together with docker-compose.yml:
#   docker compose -p sieve-hll-go-stage -f docker-compose.yml -f docker-compose.stage.yml up -d --build
#
# Fully isolated from DEV: different container names, published ports,
# and network, so `make dev-up` and `make stage-up` can run at the same
# time without colliding. Env layering adds an optional .env.stage on
# top of .env.example -> .env, for stage-only overrides (e.g. IMAGE_TAG).
services:
  redis:
    container_name: sieve-hll-go-redis-stage
    env_file:
      - path: .env.example
        required: true
      - path: .env
        required: false
      - path: .env.stage
        required: false
    ports:
      - "6381:6379"
    networks:
      - sieve-hll-go-stage-net

  app:
    container_name: sieve-hll-go-app-stage
    env_file:
      - path: .env.example
        required: true
      - path: .env
        required: false
      - path: .env.stage
        required: false
    ports:
      - "8081:8081"
    networks:
      - sieve-hll-go-stage-net

networks:
  sieve-hll-go-net:
    driver: bridge
  sieve-hll-go-stage-net:
    name: sieve-hll-go-stage-net
    driver: bridge
